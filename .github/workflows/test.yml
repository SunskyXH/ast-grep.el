name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        emacs-version:
          - "28.2"
          - "29.4"
          - "30.1"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Emacs
        uses: purcell/setup-emacs@master
        with:
          version: ${{ matrix.emacs-version }}

      - name: Install Nix
        uses: cachix/install-nix-action@v27

      - name: Install ast-grep
        run: |
          nix profile install nixpkgs#ast-grep
          ast-grep --version

      - name: Run all tests
        run: ./run-tests.sh all

      - name: Test with Emacs daemon (simulates user interaction)
        run: |
          # Start Emacs daemon to simulate user-like environment
          emacs --daemon
          
          # Load package and run tests through emacsclient
          emacsclient -e "(progn \
            (add-to-list 'load-path \"$(pwd)\") \
            (require 'ast-grep) \
            (message \"ast-grep loaded successfully in daemon mode\"))" || true
          
          # Test that the package can be loaded in daemon mode
          emacsclient -e "(featurep 'ast-grep)" | grep -q "t" && echo "✓ Package loads in daemon mode"
          
          # Stop daemon
          emacsclient -e "(kill-emacs)"
